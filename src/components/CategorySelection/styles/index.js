import styled from "styled-components";
import Button from "../../Button";

export const StyledCategorySelectionWrapper = styled.div`
  display: flex;
  flex-wrap: wrap;
  gap: 1rem;
  justify-content: space-around;
  margin-top: 1rem;
`;

export const StyledCatalogButton = styled(Button)`
  width: 100px;
`;
